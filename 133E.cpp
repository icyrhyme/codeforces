#include <cstdio>
#define MAXL 64
using namespace std;
int main() {
	char str[MAXL], n;
	scanf("%s%d", str, &n);
	int len = strlen(str);
	for(int i = 0; i < len; ++i) {
		for(int j = 0; j < n; ++j) {
		}
	}
}
